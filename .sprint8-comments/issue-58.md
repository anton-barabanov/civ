## Дизайн-ревью (архитектор) — S8-3 Карта 36×24 + производительность рендера, волна 3 из 5 (после #57; → #59∥#60 → #61)

РЕШЕНИЕ ВЛАДЕЛЬЦА ПРОДУКТА (обязательно, перекрывает тело issue): 36×24 — ЗАМЕНА размерности везде, НЕ опция старта. Переключатель «26×18 / 36×24» из тела issue НЕ делаем, параметр newGame({mapW, mapH}) НЕ вводим (остаётся newGame(diff, opponents, humans) как есть). Старые сейвы несовместимы: SAVE_KEY → "civ2_save", миграций НЕТ — load() просто не находит старый ключ, начинается новая игра. Критерий тела issue «старые сейвы грузятся» ОТМЕНЁН этим решением.

### Ключевые решения

1. **Аудит хардкода 26/18/468 — фактический по чтению кода** (заявка «38 точек в core.js» не подтверждается — ядро чистое, реальные точки ниже):
   - core.js: единственная точка литералов — строка 171 `const W = 26, H = 18, TS = 34;` → `const W = 36, H = 24, TS = 34;`. Все ~59 использований W и ~45 H идут через константы (key:178, inMap:179, floodComponents:253+, generateMap:358+, newGame:504-525, findStarts:441+, computeVision:597, mapValue:2233, offerDeal:2351/2357, recomputeBorders:883 и др.) — после смены 171 следуют автоматически. Отдельно — ПОВЕДЕНЧЕСКИЕ константы, привязанные к площади 468 (п.3).
   - renderer3d.js: 2 точки — `let mapW = 26; let mapH = 18;` (16-17, дефолты до первого draw) → `let mapW = 0; let mapH = 0;` (fitLimits:66-77 уже гвардит `!mapW`).
   - app.js: чист — импорт W/H (строка 2), использование 53, 56-57, 91-92 (vm.W/vm.H).
   - renderer2d.js: чист — импорт W/H (1), cv.width = W*TS (6-7).
   - models3d.js: 0 точек.
   - test/run.mjs: 138 вхождений литералов 26/18/468 + 2 производных в DOM-заглушке (14-16: width 884 = 26*34, height 612 = 18*34) — прямое следствие, обновляем здесь (п.6).
2. **SAVE_KEY** (172): "civ1_save" → "civ2_save". Экспорт SAVE_KEY уже есть (2722) — приложение следует само.
3. **Масштабируемые константы** (площадь 468 → 864, ×1.846; правило: то, что считало «на карту», масштабируем; то, что зависит от городов/игроков, — нет):
   - placeCamps want (1989): `3/4/5` по сложности → `[5, 7, 8]` (easy/normal/hard).
   - Минимальная компонента для лагерей `comp.cells.length < 25` (1992) → 45; гарантия ресурсов `>= 25` (323) → 45.
   - generateMap: carveChannel порог `comp.size < 90` (337) → 165; минимальные компоненты `comps[0] >= 45 / comps[1] >= 25` (380) → 80/45 (и то же в фолбэк-ветке 391-393).
   - scatterLandResources targets (310): iron 6 / horses 5 / marble 4 → 11/9/7 (та же плотность на клетку).
   - findStarts minD-лестница `[6, 4, 2]` (453) → `[7, 5, 3]`; фолбэк-старты (447) уже через W/H.
   - Дистанции лагерей (≥6 от юнитов 2005, ≥5 между лагерями 2006) — оставить: на большей карте ограничение только мягче.
   - Предельные размеры армии ИИ — НЕ масштабировать (лимит определяется городами/экономикой, не картой).
   - Стартовые юниты — те же (поселенец + воин, 552-553).
   - makeNoise(7,5)/(13,9) (361-362) — НЕ трогать: сэмплы в нормализованных координатах ×6/×4, масштаб фич следует за картой автоматически; порог суши 0.47 (372) — перцентиль, масштабируется сам.
4. **debugApi**: добавить `get W() { return W; }, get H() { return H; }` — якорь для тестов (и для #56/#57, которые уже писали проверки через api.S.map.length).
5. **renderer3d — инстансинг плит** (по коду syncTiles:263-325 и models3d.createTerrainMesh:120-170: меш = плита Box 1×0.12×1 (вода: тело + волновая верхушка) + декор: лес 132-148, холмы 149-157, горы 158-168): инстансить ТОЛЬКО плиты — один InstancedMesh на тип местности (6 штук: ocean/grass/plains/forest/hills/mountain), декор — как сейчас поперс-объектно (лес/холмы/горы — меньшинство клеток). Туман: сейчас свап материалов per-kid (289-292) — для плит перейти на instanceColor (базовый цвет × 0.45), декор сохраняет dimOf-свап. Океан: инстансная плита статична, волновая верхушка остаётся поперс-мешом (её y-анимация в oceanGroups, 624-627). Пикинг: pickMeshes (323-324) сейчас = все kid-меши; после — raycast по InstancedMesh (у THREE intersect отдаёт instanceId → тайл через per-type список индексов) + декор-меши как сейчас. Отчёт по вершинному бюджету: снять renderer.info.render (calls/triangles) до/после на разведанной карте, приложить в комментарии задачи; целевой ориентир — единицы draw calls на плиты вместо сотен. R_MAX=40 (71) достаточно (для 36: (36/2+0.2)/(tan(22.5°)*aspect) ≈ 29 при aspect 1.5). Границы syncBorders (533-573) уже LineSegments на владельца — не трогать.
6. **Тесты**: в test/run.mjs ввести `const TW = api.W, TH = api.H;` и заменить все 138 литералов (26→TW, 18→TH, 468→TW*TH) и 884/612 в заглушке (→ TW*34, TH*34 или через TS). Ожидания размеров (map.length, tileOwner.length, explored и т.п.) — через TW*TH. Матрицу «оба размера» из тела issue НЕ делаем (размер один — решение владельца); вместо неё новые проверки: findStarts на 36×24 (все старты passable, попарные дистанции respected по лестнице [7,5,3]), лагеря want=5/7/8 и дистанции, гарантия ресурсов во всех компонентах ≥45, полный endTurn-smoke. Бюджет времени (3045: <60с) — площадь ×1.85; следить за CIV_FAST-путём, при необходимости сократить число циклов генерации в тестах.

### Порядок правки (мерж-точки)
core.js: 171-172 (W/H + SAVE_KEY) → поведенческие константы (310, 323, 337, 380, 391-393, 1989, 1992, 453) → debugApi (W/H getters). renderer3d.js: 16-17 → syncTiles (инстансинг плит + instanceColor + пикинг) → отчёт производительности в комментарии. test/run.mjs: свип 138+2 литералов + новые проверки 36×24. app.js / renderer2d.js / models3d.js — без правок.

### Запреты мутаций (a-o)
- **n**: `const BARB_SPAWN_EVERY = [12, 8, 6];` (55) — дословно (масштабируем число лагерей want:1989, НЕ таймер спавна).
- **b/d**: processEconomy-хвост и пара aiDiplomacy/aiTurn в endTurn — ноль вставок.
- **e**: `3 + Math.floor(c.pop / 2) + tradeGold` (1143) — не задета.
- **l**: mapValue (2235) — цикл `W * H` меняет значение, но строка-цель мутации (`return n ? Math.max(1, Math.round(n * 0.5)) : 0;`) не трогается.
- **a/c/f/g/h/i/j/k/m/o** — не задеты.

### Риски
- Производительность renderer3d — главный риск волны: инстансинг плит + пикинг через instanceId — новая подсистема; FPS-критерий (50+ на среднем железе) меряется вручную (headless недоступен), отчёт приложить к задаче; при провале — фолбэк на текущий поперс-рендер с merged-геометрией на тип.
- findStarts на новой генерации: лестница [7,5,3] может релаксировать старты ближе — тест попарных дистанций + ручной прогон 5+ игроков.
- Время полного прогона тестов растёт ~×1.85 — держать бюджет 60с (3045).
- Потеря старых сейвов (civ1_save → civ2_save) — неизбежна по решению владельца; отразить в changelog/коммите.
